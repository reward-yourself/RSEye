# SimpleRSIandEyeDefender

Freeze screen `x`(seconds) every `y`(minutes) and `z`(minutes) every `t`(hour).

Default values:
- `x` = 30(seconds)
- `y` = 10(minutes)
- `z` = 10(minutes)
- `t` = 1(hour)
